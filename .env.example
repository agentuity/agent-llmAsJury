# No provider API keys are required when running through Agentuity:
#   bun run dev
#
# Agentuity injects AI Gateway credentials automatically.
# Do not set OPENAI_API_KEY or ANTHROPIC_API_KEY unless you explicitly
# want to bypass the Agentuity AI Gateway and call providers directly.
